services:
  - type: web
    name: antigravity-syntra-ed
    env: node
    plan: free
    buildCommand: npm install --omit=optional && npm run build
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18.0.0
